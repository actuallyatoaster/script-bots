# Write your script here!
# If you don't know how, check out the help screen

if FIRST_CALL
    #Initialize any variables here, only called once

endif
else
    #Define how your bot will behave here (called about 20 times/sec)
    move.direction = move.direction + (pi * D_TIME)
    move.speed = 1
endelse